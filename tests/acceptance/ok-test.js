import { module, test } from 'qunit';
import { setupApplicationTest } from 'ember-qunit';

module('Acceptance | OK | RFC232', function(hooks) {
  setupApplicationTest(hooks);

  test('Acceptance ok | RFC232', async function(assert) {
    assert.ok(true);
  });
});
