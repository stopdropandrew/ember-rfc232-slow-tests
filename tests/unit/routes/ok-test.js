import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Unit | Route | ok | RFC232', function(hooks) {
  setupTest(hooks);

  test('it exists', function(assert) {
    assert.ok(true);
  });
});
